# this .gitignore should be in $HOME, the working directory for .dotfiles.git
# .dotfiles.git should live in a bare repository inside the working directory

# ignore everything in root of $HOME including:
# regular directories, files, etc.
# untracked dotfiles (.ssh, .rubies, .gem, etc.)
# the repo should also ignore itself (.dotfiles.git)
/*

# don't ignore this .gitignore
!.gitignore

# don't ignore the main dotfiles (below)
